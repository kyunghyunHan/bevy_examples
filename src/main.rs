use bevy_examples::{background, basic, character, keyboard, moving, network, physics};

fn main() {
    // basic::cookbook::frame::example();
    basic::cookbook::_2d_game::example();
    basic::cookbook::entity::example();
    // physics::basic::ball_collisions::example();
    // character::example();
    // network::udp::example();
    // keyboard::example();
}
